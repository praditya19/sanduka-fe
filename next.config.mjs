/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.blibli.com",
      "media.suara.com",
      "via.placeholder.com",
      "picsum.photos",
    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
